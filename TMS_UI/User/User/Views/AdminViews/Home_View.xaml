    <!-- HOME-VIEW FOR ADMIN -->
    <UserControl x:Class="User.Views.AdminViews.Home_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:local="clr-namespace:User.Views.AdminViews"
             xmlns:locals="clr-namespace:User.Views"
             
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Label Content="HOME (ADMIN)"/>
        <Button Content="go to select directory" Height="50" Width="300" Command="{Binding NavigateSelectDirectoryCommand}"/>
        <Button Content="go to settings" Height="50" Width="150" Command="{Binding NavigateSettingsCommand}" VerticalAlignment="Top"/>
        <Button Content="go to login" Command="{Binding NavigateLoginCommand}" VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
